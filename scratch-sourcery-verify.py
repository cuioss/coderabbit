"""Throwaway file to verify Sourcery review formatting under the new dashboard
settings. This PR is not for merge — it is closed after the review is inspected."""


def greet(name):
    message = "Hello, " + name + "!"
    return message
